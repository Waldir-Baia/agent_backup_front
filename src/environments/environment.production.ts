export const environment = {
  /**
   * Production Supabase project URL. Keep this in sync with your deployment target.
   */
  supabaseUrl: '',
  /**
   * Production Supabase anon/public API key. Use environment-specific values.
   */
  supabaseAnonKey: ''
} as const;

